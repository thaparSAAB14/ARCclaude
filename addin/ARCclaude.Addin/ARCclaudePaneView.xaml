<UserControl x:Class="ARCclaude.Addin.ARCclaudePaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="BridgeGrad" MappingMode="Absolute" StartPoint="250,210" EndPoint="550,180">
            <GradientStop Color="#8B5CF6" Offset="0.0"/>
            <GradientStop Color="#3B82F6" Offset="0.5"/>
            <GradientStop Color="#10B981" Offset="1.0"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="SetuTextGrad" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#EC4899" Offset="0.0"/>
            <GradientStop Color="#8B5CF6" Offset="1.0"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="ArcTextGrad" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#3B82F6" Offset="0.0"/>
            <GradientStop Color="#10B981" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="StatusIndicatorStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Height" Value="10"/>
            <Setter Property="Fill" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Listening}" Value="True">
                    <Setter Property="Fill" Value="#10B981"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#10B981" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                        </Setter.Value>
                    </Setter>
                    <DataTrigger.EnterActions>
                        <BeginStoryboard x:Name="PulseStoryboard">
                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="1.0" To="0.35" Duration="0:0:0.9"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="PulseStoryboard"/>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#059669" Offset="0.0"/>
                                            <GradientStop Color="#10B981" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#475569"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#047857" Offset="0.0"/>
                                            <GradientStop Color="#059669" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#334155"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#64748B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#0B0F19" Offset="0.0"/>
                <GradientStop Color="#111827" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <DockPanel Margin="12">
            <!-- Header Card Panel -->
            <Border DockPanel.Dock="Top" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <Viewbox Width="46" Height="26" Margin="0,0,10,0" VerticalAlignment="Center">
                            <Canvas Width="690" Height="390">
                                <Canvas.RenderTransform>
                                    <TranslateTransform X="-55" Y="-55"/>
                                </Canvas.RenderTransform>
                                <Path Stroke="{StaticResource BridgeGrad}" StrokeThickness="12" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 250,210 C 330,110 470,110 550,180"/>
                                <Path Stroke="{StaticResource BridgeGrad}" StrokeThickness="7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0.7" Data="M 270,225 C 340,130 460,130 535,195"/>
                                <Path Stroke="#8B5CF6" StrokeThickness="10" Data="M 250,120 Q 250,250 120,250 Q 250,250 250,380 Q 250,250 380,250 Q 250,250 250,120 Z"/>
                                <Path Stroke="#EC4899" StrokeThickness="9" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 250,120 L 250,85 M 250,380 L 250,415 M 120,250 L 85,250 M 380,250 L 415,250"/>
                                <Ellipse Canvas.Left="228" Canvas.Top="228" Width="44" Height="44" Fill="#E879F9"/>
                                <Ellipse Canvas.Left="430" Canvas.Top="130" Width="240" Height="240" Stroke="#3B82F6" StrokeThickness="11"/>
                                <Ellipse Canvas.Left="495" Canvas.Top="130" Width="110" Height="240" Stroke="#3B82F6" StrokeThickness="7" Opacity="0.85"/>
                                <Path Stroke="#10B981" StrokeThickness="9" Opacity="0.9" Data="M 430,250 L 670,250"/>
                                <Path Stroke="#10B981" StrokeThickness="7" Opacity="0.85" Data="M 445,190 Q 550,215 655,190 M 445,310 Q 550,285 655,310"/>
                            </Canvas>
                        </Viewbox>
                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="SETU" FontSize="17" FontWeight="Black" Foreground="{StaticResource SetuTextGrad}"/>
                                <TextBlock Text=" / " FontSize="17" FontWeight="Light" Foreground="#9CA3AF"/>
                                <TextBlock Text="ARCCLAUDE" FontSize="17" FontWeight="ExtraBold" Foreground="{StaticResource ArcTextGrad}"/>
                            </StackPanel>
                            <TextBlock Text="AI cowork link for ArcGIS Pro" FontSize="10" Foreground="#64748B"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,16">
                        <Ellipse Style="{StaticResource StatusIndicatorStyle}"/>
                        <TextBlock Text="{Binding StatusText}" Foreground="#E2E8F0" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ToggleButton IsChecked="{Binding Listening, Mode=TwoWay}" 
                                      Style="{StaticResource ModernToggleButton}"
                                      Content="Cowork Mode" Grid.Column="0" Margin="0,0,4,0" HorizontalAlignment="Stretch"/>
                        <Button Command="{Binding ClearCommand}" 
                                Style="{StaticResource ModernButton}"
                                Content="Clear Log" Grid.Column="1" Margin="4,0,0,0" HorizontalAlignment="Stretch"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- Activity Log Card -->
            <Border Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="SESSION ACTIVITY LOG" FontSize="10" FontWeight="Bold" Foreground="#64748B" Margin="4,0,0,8"/>
                    <ListBox ItemsSource="{Binding Activity}" FontFamily="Consolas" FontSize="11"
                             Background="Transparent" BorderThickness="0" Foreground="#F1F1F1"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Timestamp}" Foreground="#475569" Margin="0,0,10,0" Grid.Column="0" VerticalAlignment="Top"/>
                                    <TextBlock Text="{Binding Message}" Foreground="{Binding Color}" FontWeight="Medium" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Top"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </DockPanel>
    </Grid>
</UserControl>
