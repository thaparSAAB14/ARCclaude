<UserControl x:Class="ARCclaude.Addin.ARCclaudePaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="300">
  <DockPanel Margin="8">
    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
      <TextBlock Text="ARCclaude" FontSize="16" FontWeight="Bold" />
      <TextBlock Text="{Binding StatusText}" Margin="0,4,0,8" />
      <StackPanel Orientation="Horizontal">
        <ToggleButton IsChecked="{Binding Listening, Mode=TwoWay}" Padding="12,4"
                      Content="Cowork mode" />
        <Button Command="{Binding ClearCommand}" Content="Clear log" Margin="8,0,0,0" Padding="12,4" />
      </StackPanel>
    </StackPanel>
    <ListBox ItemsSource="{Binding Activity}" FontFamily="Consolas" FontSize="11"
             ScrollViewer.HorizontalScrollBarVisibility="Auto" />
  </DockPanel>
</UserControl>
